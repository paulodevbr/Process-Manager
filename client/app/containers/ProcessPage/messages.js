/*
 * ProcessPage Messages
 *
 * This contains all the text for the ProcessPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProcessPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProcessPage container!',
  },
});
